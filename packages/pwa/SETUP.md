# BluePilot PWA - Setup & Run Guide

## 🚀 Quick Start

### 1. Install Dependencies
```bash
cd packages/pwa
npm install
```

### 2. Configure WalletConnect (Optional)
Edit `src/lib/wagmi.ts` and replace `YOUR_PROJECT_ID` with your WalletConnect project ID from https://cloud.walletconnect.com

### 3. Run Development Server
```bash
npm run dev
```

Visit http://localhost:5173

### 4. Build for Production
```bash
npm run build
npm run preview
```

## 📁 What Was Created

### Core Files
- ✅ `package.json` - Updated with all dependencies
- ✅ `vite.config.ts` - Configured with PWA plugin
- ✅ `tailwind.config.js` - Neo-brutalism + sky blue theme
- ✅ `postcss.config.js` - PostCSS configuration

### Source Files
```
src/
├── App.tsx                      # Main app with routing
├── main.tsx                     # Entry point
├── index.css                    # Global styles + Tailwind
├── components/
│   ├── AppLayout.tsx           # Layout with bottom nav
│   └── ui/
│       ├── Button.tsx          # Neo-brutal button
│       ├── Card.tsx            # Neo-brutal card
│       ├── Input.tsx           # Form input
│       ├── Badge.tsx           # Status badge
│       └── Modal.tsx           # Modal dialog
├── pages/
│   ├── LandingPage.tsx         # Landing page with plane animation
│   ├── HomePage.tsx            # Portfolio view
│   ├── TradePage.tsx           # Trade simulator
│   ├── PolicyPage.tsx          # Policy settings
│   └── HistoryPage.tsx         # Transaction history
├── contexts/
│   └── AppContext.tsx          # Global state management
└── lib/
    ├── types.ts                # TypeScript types
    ├── mockData.ts             # Mock tokens & transactions
    ├── simulation.ts           # Trade simulation engine
    └── wagmi.ts                # RainbowKit config
```

### Public Assets
- ✅ `public/plane.svg` - Plane icon for theme
- ✅ `public/pwa-192x192.png` - PWA icon (192x192)
- ✅ `public/pwa-512x512.png` - PWA icon (512x512)

## 🎨 Design Features

### Neo-Brutalism Style
- Thick black borders (3-4px)
- Bold shadows (4px/8px offset)
- High contrast colors
- Flat, 2D aesthetic

### Sky Blue Theme
- Primary: #0EA5E9
- Gradients: sky-100 to sky-300
- Plane motif throughout

### Typography
- Headers: Inter Bold
- Body: Inter Regular
- Mono: Roboto Mono (addresses/hashes)

## 🔧 Key Features Implemented

### Landing Page
- Animated plane flying across sky
- Hero section with tagline
- 4 feature cards
- "Enter App" CTA button

### Home Screen
- Total portfolio value
- Token list with balances
- Recent trades (last 3)
- RainbowKit connect button

### Trade Screen
- Token selector (From/To)
- Amount input with MAX button
- Simulate Trade button
- Results: output, slippage, gas, policy violations
- Execute Trade with educational modal
- Pending → Confirmed flow (2.5s delay)

### Policy Screen
- Max Slippage input
- Max Trade Size input
- Cooldown Period input
- Token allowlist checkboxes
- Update Policy button with toast
- Policy explanation card

### History Screen
- Transaction list with cards
- Status badges (confirmed/pending/failed)
- Copy transaction hash
- Basescan links
- Pull-to-refresh (↻ button)

## 🔌 Wallet Integration

- RainbowKit for wallet connection
- Base Mainnet (Chain ID: 8453)
- Base Sepolia testnet support
- View-only (no real transactions)

## 📦 PWA Features

- Service worker (auto-generated by vite-plugin-pwa)
- Manifest.json with app metadata
- Installable on mobile/desktop
- Offline support
- Theme color: #0EA5E9

## 🎭 Demo Mode

All blockchain interactions are simulated:
- Mock tokens: ETH, USDC, WETH
- Mock balances and prices
- Simulated trade calculations
- Generated transaction hashes
- Educational modal on first trade execution

## 🐛 Troubleshooting

### Dependencies not installing?
```bash
rm -rf node_modules package-lock.json
npm install
```

### Port 5173 already in use?
```bash
npm run dev -- --port 3000
```

### Build errors?
Check that all imports are correct and TypeScript is happy:
```bash
npx tsc --noEmit
```

## 📱 Testing PWA

### Desktop (Chrome)
1. Run `npm run build && npm run preview`
2. Open DevTools → Application → Manifest
3. Click "Install" icon in address bar

### Mobile
1. Deploy to a hosting service (Vercel, Netlify)
2. Visit on mobile browser
3. Tap "Add to Home Screen"

## 🚢 Deployment

### Vercel (Recommended)
```bash
npm install -g vercel
vercel
```

### Netlify
```bash
npm run build
# Upload dist/ folder to Netlify
```

### GitHub Pages
```bash
# Update vite.config.ts with base: '/repo-name/'
npm run build
# Deploy dist/ to gh-pages branch
```

## 📝 Next Steps

1. Get WalletConnect Project ID
2. Replace placeholder icons with proper PNG files
3. Test on mobile devices
4. Deploy to production
5. Share demo link!

## 🎯 Demo Flow

1. **Landing** → Click "Enter App"
2. **Home** → View portfolio
3. **Trade** → Select tokens, simulate, execute
4. **Policy** → Adjust settings
5. **History** → View transactions

Enjoy your BluePilot PWA showcase! ✈️
